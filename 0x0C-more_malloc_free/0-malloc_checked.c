#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * malloc_checked - checks sign
 * @b: int
 */
void *malloc_checked(unsigned int b)
{
	if (b > 0)
	{
	return (98);
	}
	return (malloc(b);
}
